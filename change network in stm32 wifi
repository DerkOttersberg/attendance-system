nano /etc/wpa_supplicant/wpa_supplicant-wlan0.conf



✅ Correct way to update PSK for the new password

Your new password is:

WLanVanBitsEnBytes

1️⃣ Generate a new hashed PSK

Run this exact command:

wpa_passphrase "BitsEnBytes" "WLanVanBitsEnBytes"


You’ll see output like:

network={
    ssid="BitsEnBytes"
    #psk="WLanVanBitsEnBytes"
    psk=NEWLONGHEXSTRINGHERE
}


👉 Copy only the psk=... hex line
(do NOT use the commented plaintext line)

2️⃣ Update your config file

Edit:

nano /etc/wpa_supplicant/wpa_supplicant-wlan0.conf


Replace the psk= line so it looks like:

network={
    ssid="BitsEnBytes"
    psk=NEWLONGHEXSTRINGHERE
}


Save & exit.

3️⃣ Restart Wi-Fi
systemctl restart wpa_supplicant@wlan0
systemctl restart udhcpc-wlan0

4️⃣ Verify
ip addr show wlan0
ping -c 3 google.com